<!-- app/views/machines/exercises/index.html.erb -->
<h1>Exercícios para <%= @machine.name %></h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Grupo Muscular</th>
      <th>Descrição</th>
    </tr>
  </thead>
  <tbody>
    <% @exercises.each do |exercise| %>
      <tr>
        <td><%= exercise.name %></td>
        <td><%= exercise.muscle_group %></td>
        <td><%= exercise.description %></td>
      </tr>
    <% end %>
  </tbody>
</table>
