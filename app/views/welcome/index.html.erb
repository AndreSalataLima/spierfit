<div class="flex flex-col items-center justify-center min-h-screen bg-gray-900 text-white px-4">
  <div class="w-full max-w-md space-y-8">
    <%= image_tag 'logo_300x175.png', alt: 'Logo SpierFit', class: 'w-full max-w-xs mx-auto mb-8' %>
    <h1 class="text-3xl font-bold text-center">Bem-vindo ao SpierFit!</h1>
    <p class="text-center text-lg">Acesse sua conta como Atleta abaixo:</p>

    <!-- Botão para login de atleta -->
    <div class="text-center max-w-md mx-auto">
      <%= image_tag 'logo_300x175.png', alt: 'Logo SpierFit', class: 'mx-auto mb-8' %>
      <h1 class="text-3xl font-bold text-white">Bem-vindo ao SpierFit!</h1>
      <p class="text-white mt-4 text-lg">Acesse sua conta como Atleta abaixo:</p>

      <div class="mt-8">
        <%= link_to 'Entrar como Atleta', new_user_session_path, data: { turbo: false }, class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full' %>
      </div>

    </div>


</div>

<footer class="text-center mt-8">
  <!-- Footer com links comentados -->
  <!--
  <p class="text-sm">Personal Trainer? <a href="<%= new_personal_session_path %>" class="text-blue-500 hover:underline">Faça login</a> ou <a href="<%= new_personal_registration_path %>" class="text-blue-500 hover:underline">Cadastre-se</a>.</p>
  <p class="text-sm">Academia? <a href="<%= new_gym_session_path %>" class="text-blue-500 hover:underline">Faça login</a> ou <a href="<%= new_gym_registration_path %>" class="text-blue-500 hover:underline">Cadastre-se</a>.</p>
  -->
</footer>
